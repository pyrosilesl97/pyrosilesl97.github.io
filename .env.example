# Public build-time vars for the portfolio (safe to expose in the browser).
# Secrets for Turnstile verification and Telegram live in the Cloudflare Worker
# — see workers/contact/README.md

VITE_TT_SITEKEY=
VITE_CONTACT_API_URL=
